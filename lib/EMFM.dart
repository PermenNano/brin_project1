import 'package:flutter/material.dart';

class EMFM extends StatelessWidget {
  const EMFM({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('EMFM')),
      body: const Center(child: Text('Welcome to EMFM Page')),
    );
  }
}
